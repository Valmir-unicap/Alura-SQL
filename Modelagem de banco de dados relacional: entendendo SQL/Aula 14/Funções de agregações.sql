
SELECT MAX(quantidade_venda) FROM vendas; # Consulta o maior valor na tabela quantidade
SELECT MIN(quantidade_venda) FROM vendas; # Consulta o menor valor na tabela quantidade
SELECT AVG(quantidade_venda) FROM vendas; # Realiza a média aritmética dos valores de uma determinada coluna
SELECT SUM(quantidade_venda) FROM vendas; # Calcula o somatório dos valores de um campo específico;
SELECT COUNT(quantidade_venda) FROM vendas; # Contabiliza a quantidade de linhas selecionadas
